---
apiVersion: v1
kind: Service
metadata:
  name: ajasta-postgres
  namespace: ajasta
  labels:
    app: ajasta
    component: database
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: ajasta
    component: database
