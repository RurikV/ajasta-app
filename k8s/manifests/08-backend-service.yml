---
apiVersion: v1
kind: Service
metadata:
  name: ajasta-backend
  namespace: ajasta
  labels:
    app: ajasta
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8090
    targetPort: 8090
    protocol: TCP
    name: http
  selector:
    app: ajasta
    component: backend
