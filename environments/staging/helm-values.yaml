# Staging Environment Helm Values
# Environment: staging
# Domain: staging.ajasta.top

# Global settings
global:
  namespace: ajasta-staging
  environment: staging

# Namespace configuration
namespace:
  create: true
  name: ajasta-staging
  labels:
    name: ajasta-staging
    app: ajasta
    environment: staging

# PostgreSQL Database configuration
postgres:
  enabled: false  # Using CloudNativePG instead
  # Note: PostgreSQL is deployed via CloudNativePG operator
  # See ansible playbook: 21-deploy-postgres-cluster.yml

# Backend API configuration
backend:
  enabled: true
  image:
    repository: vladimirryrik/ajasta-backend
    tag: alpine
    pullPolicy: IfNotPresent

  replicas: 1

  service:
    name: ajasta-backend
    port: 8090
    targetPort: 8090
    type: ClusterIP

  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"
      ephemeral-storage: "1Gi"
    limits:
      memory: "512Mi"
      cpu: "500m"
      ephemeral-storage: "2Gi"

  # Database connection configuration (CloudNativePG)
  database:
    url: jdbc:postgresql://cluster-postgresql-staging-rw.postgresql-cluster:5432/ajastadb
    username: ajasta
    password: autogenerated-by-cnpg

  # Application secrets
  secrets:
    jwtSecret: staging-jwt-secret-change-me-2024
    mailUsername: ""
    mailPassword: ""
    awsAccessKeyId: ""
    awsSecretAccessKey: ""
    stripePublicKey: ""
    stripeSecretKey: ""

  # Application configuration
  config:
    awsRegion: us-east-1
    awsS3Bucket: ""
    javaOpts: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"

  livenessProbe:
    tcpSocket:
      port: 8090
    initialDelaySeconds: 60
    periodSeconds: 30
    timeoutSeconds: 10
    failureThreshold: 3

  readinessProbe:
    tcpSocket:
      port: 8090
    initialDelaySeconds: 60
    periodSeconds: 10
    timeoutSeconds: 10
    failureThreshold: 6

  # Node affinity to avoid control-plane nodes
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: node-role.kubernetes.io/control-plane
            operator: DoesNotExist

# Frontend configuration
frontend:
  enabled: true
  image:
    repository: vladimirryrik/ajasta-frontend
    tag: alpine
    pullPolicy: IfNotPresent

  replicas: 1

  service:
    name: ajasta-frontend
    port: 80
    targetPort: 80
    type: ClusterIP

  resources:
    requests:
      memory: "64Mi"
      cpu: "100m"
      ephemeral-storage: "500Mi"
    limits:
      memory: "128Mi"
      cpu: "200m"
      ephemeral-storage: "1Gi"

  livenessProbe:
    httpGet:
      path: /
      port: 80
    initialDelaySeconds: 30
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 3

  readinessProbe:
    httpGet:
      path: /
      port: 80
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3

  # Node affinity to avoid control-plane nodes
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: node-role.kubernetes.io/control-plane
            operator: DoesNotExist

# Ingress configuration
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
  defaultBackend:
    enabled: false
    serviceName: ajasta-frontend
    servicePort: 80
  hosts:
    - host: staging.ajasta.top
      paths:
        - path: /
          pathType: Prefix
          serviceName: ajasta-frontend
          servicePort: 80
        - path: /api
          pathType: Prefix
          serviceName: ajasta-backend
          servicePort: 8090
  tls:
    - secretName: ajasta-staging-tls
      hosts:
        - staging.ajasta.top

# RBAC configuration
rbac:
  create: true
  serviceAccount:
    create: true
    name: ajasta-sa
    annotations: {}
