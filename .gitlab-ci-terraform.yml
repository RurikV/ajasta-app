---
# GitLab CI/CD Pipeline for Terraform Operations (Shell Runner Compatible)
# This file handles Terraform infrastructure deployment for Kubernetes cluster

variables:
  # Terraform configuration
  TF_ROOT: "${CI_PROJECT_DIR}/terraform"
  TF_STATE_NAME: "production"

  # GitLab Terraform HTTP backend configuration
  TF_HTTP_ADDRESS: "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/terraform/state/${TF_STATE_NAME}"
  TF_HTTP_LOCK_ADDRESS: "${TF_HTTP_ADDRESS}/lock"
  TF_HTTP_UNLOCK_ADDRESS: "${TF_HTTP_ADDRESS}/lock"
  TF_HTTP_USERNAME: "gitlab-ci-token"
  TF_HTTP_PASSWORD: "${CI_JOB_TOKEN}"

  # Terraform CLI cache directories
  TF_PLUGIN_CACHE_DIR: "${CI_PROJECT_DIR}/.terraform.d/plugin-cache"

  # Yandex Cloud variables - use existing CI variables
  TF_VAR_yc_cloud_id: "${YC_CLOUD_ID}"
  TF_VAR_yc_folder_id: "${YC_FOLDER_ID}"
  TF_VAR_yc_token: "${YC_TOKEN}"
  TF_VAR_ssh_username: "ajasta"
  SSH_PRIVATE_KEY: "${YC_SSH_PRIVATE_KEY}"

# Base configuration for Terraform jobs (Shell Runner)
.terraform_base: &terraform_base
  before_script:
    # System detection and package installation
    - |
      echo "Detecting operating system..."
      if [[ "$OSTYPE" == "linux-gnu"* ]]; then
        echo "Linux detected"
        if command -v apt-get >/dev/null 2>&1; then
          sudo apt-get update -qq
          sudo apt-get install -y curl unzip jq openssh-client
        elif command -v yum >/dev/null 2>&1; then
          sudo yum install -y curl unzip jq openssh-clients
        elif command -v apk >/dev/null 2>&1; then
          apk add --no-cache curl unzip jq openssh-client
        else
          echo "Unknown Linux distribution"
        fi
      elif [[ "$OSTYPE" == "darwin"* ]]; then
        echo "macOS detected"
        if ! command -v jq >/dev/null 2>&1; then
          echo "Installing jq..."
          brew install jq || echo "Please install jq manually: brew install jq"
        fi
        if ! command -v terraform >/dev/null 2>&1; then
          echo "Installing Terraform..."
          brew install terraform || echo "Please install Terraform manually: brew install terraform"
        fi
      else
        echo "Unknown OS: $OSTYPE"
      fi

    # Install Terraform if not present
    - |
      if ! command -v terraform >/dev/null 2>&1; then
        echo "Installing Terraform..."
        TERRAFORM_VERSION="1.8.0"
        if [[ "$OSTYPE" == "linux-gnu"* ]]; then
          wget https://releases.hashicorp.com/terraform/${TERRAFORM_VERSION}/terraform_${TERRAFORM_VERSION}_linux_amd64.zip
          unzip terraform_${TERRAFORM_VERSION}_linux_amd64.zip
          sudo mv terraform /usr/local/bin/
        elif [[ "$OSTYPE" == "darwin"* ]]; then
          wget https://releases.hashicorp.com/terraform/${TERRAFORM_VERSION}/terraform_${TERRAFORM_VERSION}_darwin_amd64.zip
          unzip terraform_${TERRAFORM_VERSION}_darwin_amd64.zip
          sudo mv terraform /usr/local/bin/
        fi
        rm -f terraform_${TERRAFORM_VERSION}_*.zip
      fi

    # Extract SSH public key from private key
    - |
      if [ -n "${SSH_PRIVATE_KEY:-}" ]; then
        echo "🔑 Extracting SSH public key from private key..."
        echo "${SSH_PRIVATE_KEY}" | tr -d '\r' > /tmp/id_rsa
        chmod 600 /tmp/id_rsa
        if ssh-keygen -y -f /tmp/id_rsa > /tmp/id_rsa.pub 2>/dev/null; then
          export TF_VAR_ssh_public_key="$(cat /tmp/id_rsa.pub)"
          echo "✅ SSH public key extracted successfully"
        else
          echo "❌ Failed to extract SSH public key"
          export TF_VAR_ssh_public_key=""
        fi
        rm -f /tmp/id_rsa /tmp/id_rsa.pub
      else
        echo "⚠️ No SSH private key provided"
        export TF_VAR_ssh_public_key=""
      fi

    # Navigate to terraform directory
    - cd ${TF_ROOT}

    # Setup Terraform CLI config
    - export TF_CLI_CONFIG_FILE="${CI_PROJECT_DIR}/.terraformrc"

    # Create .terraformrc file
    - |
      cat > ${CI_PROJECT_DIR}/.terraformrc << 'EOF'
      provider_installation {
        network_mirror {
          url = "https://terraform-mirror.yandexcloud.net/"
          include = ["registry.terraform.io/*/*"]
        }
        direct {
          exclude = ["registry.terraform.io/*/*"]
        }
      }
      EOF

    # Create terraform.tfvars from environment variables
    - |
      cat > terraform.tfvars << 'EOF'
      # Yandex Cloud configuration - from GitLab CI variables
      yc_cloud_id  = "${TF_VAR_yc_cloud_id}"
      yc_folder_id = "${TF_VAR_yc_folder_id}"
      yc_zone      = "ru-central1-b"

      # SSH configuration - public key extracted from private key in CI/CD
      ssh_username = "ajasta"
      ssh_public_key = "${TF_VAR_ssh_public_key}"

      # Networking - using same names as manual deployment
      yc_network_name           = "external-ajasta-network"
      yc_subnet_name            = "ajasta-external-segment"
      yc_subnet_cidr            = "172.16.17.0/28"
      yc_internal_network_name  = "internal-ajasta-network"
      yc_internal_subnet_name   = "ajasta-internal-segment"
      yc_internal_subnet_cidr   = "10.10.0.0/24"

      # Static addresses - same naming convention
      master_address_name = "ajasta-k8s-master-ip"
      workers = [
        { vm_name = "k8s-worker-1", address_name = "ajasta-k8s-worker1-ip" },
        { vm_name = "k8s-worker-2", address_name = "ajasta-k8s-worker2-ip" },
        { vm_name = "k8s-worker-3", address_name = "ajasta-k8s-worker3-ip" },
      ]

      # VM specifications - same as manual deployment
      master_vm_name     = "k8s-master"
      master_vm_memory   = 6
      master_vm_cores    = 2
      master_vm_disk_size = 30
      worker_vm_memory   = 6
      worker_vm_cores    = 2
      worker_vm_disk_size = 30

      # cloud-init user-data (relative to terraform directory)
      metadata_yaml = "../scripts/metadata.yaml"
      EOF

    # Debug variables and environment check
    - echo "🔍 Debug: Environment variables check:"
    - echo "   TF_VAR_yc_cloud_id: ${TF_VAR_yc_cloud_id:0:20}..."
    - echo "   TF_VAR_yc_folder_id: ${TF_VAR_yc_folder_id:0:20}..."
    - echo "   TF_VAR_ssh_username: ${TF_VAR_ssh_username}"
    - echo "   TF_VAR_ssh_public_key: ${TF_VAR_ssh_public_key:0:30}..."
    - echo "   SSH_PRIVATE_KEY length: ${#SSH_PRIVATE_KEY}"
    - echo ""
    - echo "📄 terraform.tfvars created:"
    - cat terraform.tfvars | head -10

    # Show versions
    - terraform --version
    - jq --version

    # Initialize Terraform
    - terraform init -input=false -backend=true
      -backend-config="address=${TF_HTTP_ADDRESS}"
      -backend-config="lock_address=${TF_HTTP_LOCK_ADDRESS}"
      -backend-config="unlock_address=${TF_HTTP_UNLOCK_ADDRESS}"
      -backend-config="username=${TF_HTTP_USERNAME}"
      -backend-config="password=${TF_HTTP_PASSWORD}"
      -backend-config="lock_method=POST"
      -backend-config="unlock_method=POST"
      -backend-config="retry_wait_min=5"
  cache:
    key: terraform-${CI_COMMIT_REF_SLUG}
    paths:
      - ${TF_ROOT}/.terraform/
      - ${TF_ROOT}/.terraform.lock.hcl

# Terraform validation stage
terraform:validate:
  <<: *terraform_base
  stage: plan
  script:
    - terraform fmt -check -recursive
    - terraform validate -no-color
    - echo "Terraform validation completed successfully"
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
  allow_failure: true  # Don't block pipeline if Terraform validation fails

# Terraform planning stage
terraform:plan:
  <<: *terraform_base
  stage: plan
  script:
    - terraform plan -input=false -out=plan.tfplan
    - terraform show -no-color plan.tfplan > plan.txt
    - echo "Terraform plan created successfully"
    - ls -la plan.tfplan plan.txt
  artifacts:
    paths:
      - terraform/plan.tfplan
      - terraform/plan.txt
    expire_in: "1 week"
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
  needs: [terraform:validate]

# Terraform apply job for development (automatic)
terraform:apply:dev:
  <<: *terraform_base
  stage: deploy
  environment:
    name: terraform-dev
  script:
    - terraform apply -input=false -auto-approve plan.tfplan
    - echo "TERRAFORM_MASTER_IP=" > terraform_outputs.env
    - terraform output -raw master_public_ip >> terraform_outputs.env
    - echo "TERRAFORM_WORKER_IPS=" >> terraform_outputs.env
    - terraform output -json worker_public_ips >> terraform_outputs.env
    - echo "TERRAFORM_CLUSTER_READY=true" >> terraform_outputs.env
    - ls -la terraform_outputs.env
  artifacts:
    paths:
      - terraform/terraform_outputs.env
    reports:
      dotenv: terraform/terraform_outputs.env
  rules:
    - if: $CI_COMMIT_BRANCH == "develop"
  needs: [terraform:plan]

# Terraform apply job for production (manual deployment)
deploy:tf:production:
  <<: *terraform_base
  stage: deploy
  environment:
    name: terraform-production
    url: http://${TF_VAR_master_public_ip:-$YC_VM_EXTERNAL_IP}
    on_stop: terraform:destroy:production
  script:
    - terraform apply -input=false plan.tfplan
    - echo "TERRAFORM_MASTER_IP=" > terraform_outputs.env
    - terraform output -raw master_public_ip >> terraform_outputs.env
    - echo "TERRAFORM_WORKER_IPS=" >> terraform_outputs.env
    - terraform output -json worker_public_ips >> terraform_outputs.env
    - echo "TERRAFORM_CLUSTER_READY=true" >> terraform_outputs.env
    - echo "Production Kubernetes Cluster Created Successfully"
    - echo "Cluster ready for application deployment"
    - ls -la terraform_outputs.env
  artifacts:
    paths:
      - terraform/terraform_outputs.env
    reports:
      dotenv: terraform/terraform_outputs.env
    expire_in: "1 month"
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
  needs: [terraform:plan]
  timeout: 30m

# Update kubeconfig after Terraform deployment
k8s:update-kubeconfig:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client curl jq
    - chmod +x ../scripts/update-kubeconfig.sh
  script:
    # Wait a bit for the cluster to be ready
    - echo "⏳ Waiting 60 seconds for Kubernetes cluster to initialize..."
    - sleep 60
    # Run the kubeconfig update script
    - ../scripts/update-kubeconfig.sh "$TERRAFORM_MASTER_IP"
    # Verify the connection
    - export KUBECONFIG="$HOME/.kube/config"
    - kubectl cluster-info
    - kubectl get nodes
    - echo "✅ Kubeconfig updated and verified successfully"
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
  needs: [deploy:tf:production]
  dependencies:
    - deploy:tf:production
  timeout: 10m
  environment:
    name: terraform-production
    url: http://${TERRAFORM_MASTER_IP:-$YC_VM_EXTERNAL_IP}

# Terraform destroy job for production (manual cleanup)
terraform:destroy:production:
  <<: *terraform_base
  stage: deploy
  environment:
    name: terraform-production
    action: stop
  script:
    - terraform plan -destroy -out=destroy.tfplan
    - terraform apply -input=false -auto-approve destroy.tfplan
    - echo "Production infrastructure destroyed successfully"
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
  needs: []
  timeout: 15m
  allow_failure: true